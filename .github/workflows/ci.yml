name: Nexora CI
on:
  push:
  pull_request:

jobs:
  static-audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Static audit
        run: ./scripts/static-audit.sh
